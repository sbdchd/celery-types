#!/bin/bash

set -ex

# format code
if [[ $CI ]]; then
    ./.venv/bin/black --check .
    ./.venv/bin/ruff check .
else
    ./.venv/bin/black .
    ./.venv/bin/ruff check . --fix
fi

./.venv/bin/pyright typings tests

# type check code
./.venv/bin/mypy tests
